# 执法检测系统 使用说明

---

## 目录

1. [系统概述](#1-系统概述)
2. [环境要求](#2-环境要求)
3. [文件说明](#3-文件说明)
4. [图形界面（GUI）使用](#4-图形界面gui使用)
   - [标签页一：清算退款](#41-标签页一清算退款)
   - [标签页二：非税收入](#42-标签页二非税收入)
   - [标签页三：文书生成](#43-标签页三文书生成)
5. [命令行（CLI）使用](#5-命令行cli使用)
6. [输出结果说明](#6-输出结果说明)
7. [常见问题](#7-常见问题)

---

## 1. 系统概述

执法检测系统用于对国库集中支付零余额账户的银行流水进行自动化分析，发现以下违规行为：

- **资金匹配检测**：追踪退款资金从零余额账户→清算待转户→国库的全流程，识别占压财政资金行为
- **延迟清算积数检测**：计算未及时清算的资金积数（金额 × 延迟天数）
- **非税收入核查**：滚动匹配流入资金与划转记录，发现超期未划转的可疑资金
- **执法文书生成**：根据检测结果自动生成《事实认定书》

### 工作流程

```
银行流水文件 (.xlsx/.csv)
       ↓
  资金匹配检测 ──→ 资金匹配结果.xlsx
       ↓
  延迟清算积数检测 ──→ 延迟清算积数.xlsx
       ↓
  非税收入核查 ──→ 非税核查结果.xlsx
       ↓
  执法文书生成 ──→ 事实认定书_XXXX.docx
```

---

## 2. 环境要求

- **Python** 3.9+
- **依赖库**（见 `requirements.txt`）：

```
pip install pandas openpyxl customtkinter python-docx openai
```

- **操作系统**：Windows 10/11（推荐）或 macOS / Linux（GUI 需桌面环境）

---

## 3. 文件说明

### 输入文件

| 文件 | 格式 | 说明 |
|------|------|------|
| `零余额账户.xlsx` | Excel | 零余额账户银行流水，含交易日期、金额、对方户名等字段。表头自动识别（第10行） |
| `零余额账户.csv` | CSV | 同上，14列格式：账号、卡号、币种、日期、时间、时间戳、发生额（分）、余额、借贷标志、对方行名、对方行号、对方账号、对方户名、明细序号 |
| `集中支付零余额清算待转.xlsx` | Excel | 清算待转账户交易明细。表头自动识别（第6行） |
| `集中支付垫款户.csv` | CSV | 垫款户流水，73列格式。系统自动归一化列名 |
| `集中执法垫款户.xlsx` | Excel | 垫款户流水（与清算待转同义，名称可配置） |
| `事实认定书模板.docx` | Word | 文书模板，含 `{{truth}}` 占位符 |

### 输出文件

| 文件 | 说明 |
|------|------|
| `output/资金匹配结果.xlsx` | 资金匹配检测详情 |
| `output/零余额账户延迟清算积数.xlsx` | 延迟清算积数计算结果 |
| `output/非税核查结果.xlsx` | 非税收入核查结果 |
| `output/事实认定书_XXXX.docx` | 生成的执法文书 |

---

## 4. 图形界面（GUI）使用

启动方式：

```bash
python gui.py
```

界面分为三个标签页，底部为共享运行日志。

### 4.1 标签页一：清算退款

执行**资金匹配检测**和**延迟清算积数检测**。

**操作步骤：**

1. **选择零余额账户文件** — 点击"浏览"选择 `零余额账户.xlsx` 或 `零余额账户.csv`
2. **选择清算待转文件** — 点击"浏览"选择 `集中支付零余额清算待转.xlsx` 或 `集中支付垫款户.csv`
3. **填写垫款户名称** — 输入清算待转账户在流水中的对方户名（如 `集中支付零余额清算待转`、`集中执法垫款户`）
   > 若清算待转路径为默认值，系统会自动用此名称拼接文件名查找
4. **设置输出目录**（可选）
5. **勾选"跳过文书生成"**（可选）— 只检测不生成文书
6. 点击 **「▶ 开始检测」**

**输出：**
- `output/资金匹配结果.xlsx`
- `output/零余额账户延迟清算积数.xlsx`
- （如未跳过）`output/事实认定书_XXXX.docx`

---

### 4.2 标签页二：非税收入

执行**非税收入滚动匹配核查**。

**操作步骤：**

1. **选择流水文件** — 选择需要核查的银行流水文件（`零余额账户.xlsx`、`零余额账户.csv` 等）
2. **填写划转账户名称** — 输入指定划转账户的对方户名（如 `集中支付零余额清算待转`）
   > 对方户名为此名称的负向交易被视为"划转操作"
3. **设置阈值天数** — 流入资金超过此天数未划转即标记为"可疑"（默认 2 天）
4. 点击 **「▶ 开始核查」**

**算法说明（FIFO 先进先出）：**

```
流入资金A(100元, 第1天) ──┐
                          ├──→ 划转(150元, 第3天) → A全部匹配 + B匹配50
流入资金B(100元, 第2天) ──┘
                          └──→ B剩余50元未划转，到第5天超过2天阈值 → 标记可疑
```

**输出：**
- `output/非税核查结果.xlsx`

结果表字段：

| 字段 | 说明 |
|------|------|
| 来源日期 | 资金流入日期 |
| 来源金额 | 该日流入总金额 |
| 已匹配金额 | 已被划转抵扣的金额 |
| 未匹配金额 | 尚未划转的剩余金额 |
| 等待天数 | 从流入日到最新交易日的天数 |
| 阈值天数 | 设定的阈值 |
| 状态 | 已划转 / 未划转 / **可疑**（超阈值） |

---

### 4.3 标签页三：文书生成

从已有的资金匹配结果文件单独生成执法文书。

**操作步骤：**

1. **选择结果文件** — 选择 `资金匹配结果.xlsx`
2. **选择模板文件**（可选）— 默认使用 `事实认定书模板.docx`
3. **设置输出目录**（可选）
4. **选择文书模式**：
   - ◉ **模板填充** — 使用预定义模板，速度快
   - ○ **LLM 生成** — 使用 DeepSeek 大模型生成描述，需设置 `DEEPSEEK_API_KEY` 环境变量
5. 点击 **「📄 生成文书」**

**输出：**
- `output/事实认定书_XXXX_001.docx`
- `output/事实认定书_XXXX_002.docx`
- ...

每一条有完整退款链路（退款日期 + 退至国库日期）的记录生成一份独立的文书。

---

## 5. 命令行（CLI）使用

### 完整管线（资金匹配 + 延迟清算 + 文书生成）

```bash
# 使用默认文件路径
python main.py

# 指定输入文件
python main.py \
    --zero-balance 零余额账户.xlsx \
    --qing-suan 集中支付垫款户.csv \
    --advance-name "集中执法垫款户"

# 自定义输出目录
python main.py --output-dir ./结果输出

# 只检测不生成文书
python main.py --skip-report

# LLM 模式生成文书（需设置 API Key）
set DEEPSEEK_API_KEY=sk-xxxxx
python main.py --report-mode llm
```

### 非税收入核查

```bash
# 使用默认流水文件
python main.py --non-tax

# 指定流水文件和参数
python main.py --non-tax \
    --non-tax-file 零余额账户.csv \
    --non-tax-account "集中支付零余额清算待转" \
    --non-tax-days 3

# 仅运行非税核查（跳过其他检测）
python main.py --skip-report --non-tax
```

### 单独生成文书

```bash
# 从已有的资金匹配结果生成
python main.py --from-results output/资金匹配结果.xlsx

# 指定模板和输出目录
python main.py --from-results output/资金匹配结果.xlsx \
    --template-path ./模板/事实认定书模板.docx \
    --output-dir ./文书输出
```

### 完整 CLI 参数

```
--zero-balance PATH   零余额账户文件路径（默认: 零余额账户.xlsx）
--qing-suan PATH      清算待转文件路径（默认: 集中支付零余额清算待转.xlsx）
--advance-name NAME   垫款户名称（默认: 集中支付零余额清算待转）
--output-dir DIR      输出目录（默认: output）
--skip-report         跳过文书生成
--report-mode MODE    文书模式: template / llm（默认: template）
--template-path PATH  事实认定书模板路径

--non-tax             启用非税收入核查
--non-tax-file PATH   非税核查流水文件（默认同 --zero-balance）
--non-tax-account     非税核查指定账户名称（默认同 --advance-name）
--non-tax-days N      未划转阈值天数（默认: 2）

--from-results PATH   从结果文件生成文书（跳过检测）
```

---

## 6. 输出结果说明

### 资金匹配结果.xlsx

| 字段 | 说明 |
|------|------|
| sheet名称 | 来源Sheet |
| 账号 | 零余额账户账号 |
| 待查日期 | 退款入账日期 |
| 待查金额 | 退款金额 |
| 退款日期 | = 待查日期 |
| 清算日期 | 最近一笔从清算待转户转入的日期 |
| 退至待转户日期 | 转出至清算待转户的日期 |
| 退至国库日期 | 资金最终退回国库的日期 |

**判定规则：** 同时有**退款日期**和**退至国库日期**的记录，用于生成执法文书。有退款日期但无退至国库日期，说明资金尚未完成退库。

### 延迟清算积数.xlsx

| 字段 | 说明 |
|------|------|
| 交易日期 | 正向交易日期 |
| 交易金额 | 交易金额 |
| 清算日期 | 最终匹配的负向清算日期 |
| 日期间隔 | 延迟天数 |
| 积数 | 金额 × 延迟天数 |

### 非税核查结果.xlsx

| 字段 | 说明 |
|------|------|
| 来源日期 | 资金流入日期 |
| 来源金额 | 该日流入金额 |
| 已匹配金额 | 已被划转抵扣的金额 |
| 未匹配金额 | 尚未划转的金额 |
| 等待天数 | 等待天数 |
| 状态 | 已划转 / 未划转 / 可疑 |

---

## 7. 常见问题

### Q: 系统提示"文件不存在"

确保输入文件在工作目录下，或使用"浏览"按钮选择文件路径。支持 `.xlsx`、`.xls`、`.csv`、`.txt` 格式。

### Q: CSV 文件乱码

系统自动检测 UTF-8 / GBK / GB2312 编码。如仍有问题，请确保 CSV 文件保存为 GBK 编码。

### Q: 垫款户名称怎么填？

填写在银行流水中用于标识清算待转账户的"对方户名"。不同银行可能使用不同名称，如：
- `集中支付零余额清算待转`
- `集中执法垫款户`
- `集中支付垫款户`

可通过查看流水中的对方户名列确定。

### Q: 非税核查阈值设多少？

- **2 天**（默认）：适用于日终清算的场景
- **3~5 天**：适用于周末或节假日顺延的场景
- **7 天**：适用于按周汇总划转的场景

### Q: 文书生成提示模板不存在

确保 `事实认定书模板.docx` 文件在工作目录下。可通过 `--template-path` 或 GUI 中指定自定义模板路径。

模板中需包含以下占位符：
- `{{truth}}` — 事实描述内容（必需）
- `{{number}}` — 文书编号（可选）

### Q: LLM 文书生成需要什么？

需要设置环境变量 `DEEPSEEK_API_KEY`，值为 DeepSeek API 密钥。
