# 执法检测系统 — 项目结构文档

## 概述

**执法检测系统** 是一个财政专项资金检查辅助工具，用于从银行流水报表中自动发现违规记录并生成执法文书。

**核心业务逻辑：**
在国库集中支付制度下，零余额账户每日发生的退款应当及时清算退回国库。当退款入账后未及时退库，即构成"占压财政资金"。本系统自动匹配交易记录，计算各环节日期，标记违规行为，并生成《事实认定书》。

---

## 目录结构

```
执法/
├── main.py                          # ★ 命令行入口 — 检测+文书生成全流程
├── gui.py                           # ★ 图形界面入口（customtkinter，4个标签页）
├── process_zero_balance.py          # ★ 核心检测逻辑（资金匹配/非税核查/清算核查）
├── generate_report.py               # ★ 文书生成逻辑（模板填充 / LLM生成）
├── preprocess.py                    # ★ 流水预处理 + 延迟清算检测
├── process_zero_balance_v2.py       # 延迟清算积数检测（独立旧版，保留参考）
│
├── 修改说明.txt                     # 功能修改记录
│
├── 事实认定书模板.docx              # 执法文书 Word 模板（含 {{truth}} 占位符）
│
├── 输入文件（示例/默认）：
│   ├── 零余额账户.xlsx              # 零余额账户流水（多Sheet）
│   ├── 集中支付零余额清算待转.xlsx   # 垫款户流水（或 .csv）
│   ├── 非税专户.csv                 # 非税收入专户流水
│   ├── 集中支付垫款户.csv           # 垫款户 CSV 格式
│   ├── 零余额账户.csv               # 零余额账户 CSV 格式
│   ├── _test_sc2.csv / _test_feishui.csv  # 测试数据
│   └── 清算待转.xlsx / 零.xlsx / 11111零.xlsx  # 其他测试文件
│
├── 输出目录（默认 output/）：
│   ├── 违规记录.xlsx                # 资金匹配检测结果
│   ├── 非税核查结果.xlsx            # 非税收入核查结果
│   ├── 清算核查结果.xlsx            # 清算核查结果
│   ├── 资金匹配结果.xlsx            # 资金匹配中间结果
│   └── 零余额账户延迟清算积数.xlsx  # 延迟清算积数计算结果
│   └── 事实认定书_XXXX.docm         # 生成的执法文书
│
├── 打包结果/                        # PyInstaller 打包输出
│   ├── 64位_Win7+/
│   └── 32位_Win7_兼容/
│
├── build/                           # PyInstaller 构建缓存
├── build_win7.bat                   # 64位打包脚本
├── build_win7_32.bat                # 32位打包脚本
├── 执法检测系统_Win_x64.spec        # PyInstaller 配置
│
├── 使用说明.md / 使用说明.txt        # 用户文档
└── .claude/                         # Claude Code 配置文件
    └── settings.local.json
```

---

## 模块关系图

```
                    ┌─────────────────────────────────────────┐
                    │              main.py                    │
                    │         命令行入口/流程编排               │
                    │  argparse → run_pipeline() → 各检测模块   │
                    └────┬──────────────┬────────────────────┘
                         │              │
              ┌──────────▼──────────┐  │
              │    gui.py           │  │
              │  图形界面(4标签页)    │  │
              │  customtkinter UI   │  │
              └──────────┬──────────┘  │
                         │              │
    ┌────────────────────┼──────────────┼──────────────────────────┐
    │                    │              │                          │
    ▼                    ▼              ▼                          ▼
┌───────────────────────────────────────────────────────┐
│              process_zero_balance.py                   │
│           三类检测算法的核心实现                        │
│                                                       │
│  detect_fund_matching()        ← 资金匹配检测          │
│  detect_non_tax_verification() ← 非税收入核查          │
│  detect_settlement_verification() ← 清算核查           │
│  detect_delayed_settlement()   ← 延迟清算积数检测      │
└───────────────┬───────────────────────────────────────┘
                │
                ▼
┌───────────────────────────────────────────────┐
│            generate_report.py                  │
│        从违规记录生成事实认定书 docx            │
│                                                │
│  generate_description_template()  → 模板填充   │
│  generate_description_llm()       → LLM 生成   │
│  replace_truth_in_docx()          → 替换占位符  │
│  batch_generate()                 → 批量处理   │
└───────────────────────────────────────────────┘

┌───────────────────────────────────────────────┐
│              preprocess.py                     │
│  流水文件预处理 + 延迟清算检测                 │
│                                                │
│  preprocess_transactions()    → 按账户拆分排序  │
│  detect_end_of_day_nonzero()  → 余额未归零检测  │
└───────────────────────────────────────────────┘
```

---

## 核心模块详细说明

### 1. `main.py` — 命令行主入口

**功能：** 解析命令行参数 → 调用检测管线 → 生成文书

**主要函数：**

| 函数 | 说明 |
|---|---|
| `run_pipeline()` | 检测-文书生成全流程编排，包含步骤1资金匹配→步骤2文书生成→可选步骤3非税核查→可选步骤4清算核查 |
| `_convert_matching_to_report_rows()` | 将检测结果列名映射为文书生成器需要的格式 |
| `_summary_stats()` | 输出各环节日期字段的统计摘要 |
| `main()` | 命令行参数解析入口 |

**管线流程：**
```
输入文件 → detect_fund_matching() → 违规记录.xlsx → batch_generate() → 事实认定书.docx
                                                    ↕（可选）
                                             非税核查 / 清算核查
```

**执行方式：**
```bash
python main.py                                              # 默认路径
python main.py --zero-balance ./data/零余额.xlsx             # 指定文件
python main.py --skip-report                                 # 仅检测不生成文书
python main.py --report-mode llm                             # LLM模式生成
python main.py --from-results output/违规记录.xlsx            # 从已有结果生成文书
python main.py --non-tax --settlement-check ./流水.xlsx       # 启用附加检测
python main.py --workers 4                                   # 4进程并行处理
```

---

### 2. `gui.py` — 图形界面

**框架：** customtkinter（Tkinter 现代化主题）

**标签页：**

| 标签页 | 功能 | 对应后端函数 |
|---|---|---|
| **清算退款** | 资金匹配检测 + 文书生成 + 清算核查 | `detect_fund_matching()`, `batch_generate()`, `detect_settlement_verification()` |
| **非税收入** | 非税专户滚动匹配划转核查 | `detect_non_tax_verification()` |
| **文书生成** | 从已有结果文件生成执法文书 | `batch_generate_from_file()` |
| **流水预处理** | 多账户混合流水按账户拆分排序 + 延迟清算余额非零检测 | `preprocess_transactions()`, `detect_end_of_day_nonzero()` |

**关键组件：**
- `TextRedirector` — 将 `print()` 输出重定向到 GUI 日志区域（线程安全）
- LLM 配置弹窗 — 独立对话框配置 API Key、模型、温度等参数

---

### 3. `process_zero_balance.py` — 核心检测引擎（约1630行）

这是系统最复杂的模块，包含**四种检测算法**。

#### 3a. `detect_fund_matching()` — 资金匹配检测

**目的：** 从零余额账户中提取正向非清算交易（退款），自动匹配各环节日期。

**输入：**
- 零余额账户 xlsx（多 Sheet，每个 Sheet 一个账号的流水）
- 集中支付零余额清算待转 xlsx（垫款户流水，记录资金流转过程）

**输出列：**
| 列 | 说明 |
|---|---|
| sheet名称 | 账号/Sheet 名 |
| 账号 | 零余额账号 |
| 对方账号 / 对方户名 | 退款来源 |
| 摘要 | 交易描述 |
| 清算日期 | 垫款户向该账号拨付资金的日期（二分查找最近的一次） |
| 退款日期 | 退款入账日期 |
| 退至垫款户日期 | 资金从零余额账户转至垫款户的日期（按金额精确匹配） |
| 退至金库日期 | 资金最终退回国库的日期（余额归零或转往账待转判定） |
| 交易金额 | 退款金额 |

**匹配逻辑（三阶段）：**

1. **预建索引（O(N log N)）**
   - 清算日期候选：对方户名=垫款户且金额>0的所有日期，排序去重
   - 负向交易金额索引：按金额分组，组内日期排序

2. **提取待查记录（O(N)）**
   - 筛选金额>0且对方户名≠垫款户的交易（即退款入账）

3. **逐条匹配（O(P log N)）**
   - **清算日期**：在清算候选日期中二分查找 ≤ 退款日期的最大值
   - **退至垫款户日期**：按金额精确匹配，找 > 退款日期的最早负向交易
   - **退至金库日期**：检查当日垫款户余额是否归零或是否已转往账待转

**并行支持：** 多个 Sheet 通过 `ProcessPoolExecutor` 并行处理。

---

#### 3b. `detect_non_tax_verification()` — 非税收入核查（v2算法）

**目的：** 对非税专户流水进行滚动匹配，判断来账是否在规定工作日内划转到指定账户。

**核心规则（v2优化）：**
- **FIFO累计匹配**：不再要求 1:1 精确金额
  - 一笔来账可被多笔划转分批转出（拆分划转）
  - 一笔划转可覆盖多笔来账（合并划转）
  - 当天划转一部分，剩余第二天同新来账一起划转
- **零余额检测**：若某笔交易后余额为零，则截至当时的所有待匹配来账均视为已划转
- **工作日计算**：跳过周六周日

**输出状态：**
| 状态 | 说明 |
|---|---|
| 已划转 | 在阈值工作日内完成划转 |
| 延迟划转 | 超过阈值但最终完成划转 |
| 未划转 | 尚未划转（在途） |
| 可疑 | 超过阈值且仍未划转 |

**末尾自动附加汇总行，** 包含流入合计/已匹配合计/未匹配合计。

---

#### 3c. `detect_settlement_verification()` — 清算核查

**目的：** 对单个流水文件，检查来账后指定工作日内是否有划转到指定账户的出账。

**校验逻辑：**
1. 筛选所有来账（收入/贷，金额>0），排除对方户名==指定账户的记录（内部调拨）
2. 对每条来账，检查其日期后 `days_threshold` 个工作日内（含当天），是否存在至少一笔出账且交易对象为指定账户
3. 若不存在 → 标记为可疑

**注意：** 此检查不涉及金额匹配，仅判断是否有向指定账户的划转行为。

---

#### 3d. `detect_delayed_settlement()` — 延迟清算积数检测

**目的：** 找出未在当天或第二个工作日完成清算的交易，计算 **积数**（金额 × 延迟天数）。

**积数** 是财政检查中的术语，用于量化违规严重程度：
- 积数 = 退款金额 × 延迟清算天数
- 积数越大 → 违规越严重
- 结果按积数降序排列

---

### 4. `generate_report.py` — 文书生成

**输入：** 结构化违规记录（dict 列表）
**输出：** 事实认定书 docx 文件

**两种生成模式：**

| 模式 | 实现 | 适用场景 |
|---|---|---|
| **template** | `generate_description_template()` 用字符串模板拼接事实描述 | 快速、稳定，不依赖外部服务 |
| **llm** | `generate_description_llm()` 调用 DeepSeek API 生成描述 | 需要更自然的语言表达 |

**docx 模板机制：**
1. 模板文件 `事实认定书模板.docx` 包含 `{{truth}}` 和 `{{number}}` 占位符
2. `replace_truth_in_docx()` 遍历段落和表格，替换所有占位符
3. 支持自定义模板路径

**批量处理：**
- `max_workers > 1` 时使用 `ThreadPoolExecutor` 多线程并行生成
- `batch_generate_from_file()` 支持从已保存的 Excel 结果文件直接生成

---

### 5. `preprocess.py` — 流水预处理

**两大功能：**

#### 5a. `preprocess_transactions()` — 流水拆分排序

**场景：** 银行导出的 Excel 中多个账户的交易记录混合在同一个 Sheet 中。

**处理流程：**
1. 自动检测表头行（扫描前15行，按列名关键词评分）
2. 自动识别关键列（账户、日期、时间、余额、金额）
3. 处理"账户号仅在首行出现"的特殊格式（前向填充）
4. 按账户分组，每个账户的数据按（日期+时间）升序排列
5. 每个账户写入一个独立 Sheet

**列名兼容：**
| 标准列名 | 兼容别名 |
|---|---|
| 账户/账号 | 帐号 |
| 交易日期 | 入帐日期、入账日期、日期 |
| 交易时间 | 时间、交易时刻 |
| 余额 | 账户余额 |
| 交易金额 | 发生额、发生额（分）、金额 |

#### 5b. `detect_end_of_day_nonzero()` — 延迟清算检测

**逻辑：**
1. 读取预处理的 Excel（每个账户一个 Sheet，已排序）
2. 对每个 Sheet，按交易日期分组
3. 每个日期内按时间排序，取最后一笔交易
4. 检查该笔余额 ≠ 0 → 记录（存在延迟清算嫌疑）

---

### 6. `process_zero_balance_v2.py` — 旧版独立脚本

`detect_delayed_settlement()` 的独立实现版本。功能已被整合到 `process_zero_balance.py` 中，保留作为参考。

---

## 数据流图

```
┌─────────────────────────────────────────────────────────────────────┐
│                          执行流程（完整管线）                        │
└─────────────────────────────────────────────────────────────────────┘

 零余额账户.xlsx         集中支付清算待转.xlsx
       │                        │
       ▼                        ▼
 ┌──────────────────────────────────────┐
 │    detect_fund_matching()            │
 │    1. 预建索引（清算日期+金额索引）   │
 │    2. 提取待查退款记录                │
 │    3. 逐条匹配各环节日期              │
 └──────────────┬───────────────────────┘
                ▼
         违规记录.xlsx
         （含清算日期/退款日期/
          退至垫款户日期/退至金库日期）
                │
                ▼
 ┌──────────────────────────────────────┐
 │    _convert_matching_to_report_rows() │
 │    列名映射 + 筛选完整记录            │
 └──────────────┬───────────────────────┘
                ▼
 ┌──────────────────────────────────────┐
 │    batch_generate()                  │
 │    生成事实描述 → 替换模板占位符     │
 │    → 保存 docx                       │
 └──────────────┬───────────────────────┘
                ▼
      事实认定书_XXXX.docx
      （执法文书，每份一个文件）


 ┌──────────────────────────────────────┐
 │  可选检测：非税核查/清算核查         │
 │                                      │
 │  detect_non_tax_verification()       │
 │  → FIFO累计匹配 → 非税核查结果.xlsx  │
 │                                      │
 │  detect_settlement_verification()    │
 │  → 窗口检查 → 清算核查结果.xlsx      │
 └──────────────────────────────────────┘
```

---

## 输入输出文件规范

### 输入文件

| 文件 | 格式 | 说明 |
|---|---|---|
| 零余额账户.xlsx | Excel 多 Sheet | 每个 Sheet 一个账号，标准表头行=9 |
| 集中支付零余额清算待转.xlsx/csv | Excel/CSV | 垫款户流水，含交易日期/交易金额/对方户名/账号/余额 |
| 非税专户.csv | CSV | 非税收入流水，可含"地区号"等多列 |
| CSV 文件通用 | CSV | 自动检测编码（utf-8/gbk），自动识别分隔符，自动归一化列名 |

### 输出文件

| 文件 | 说明 |
|---|---|
| output/违规记录.xlsx | 资金匹配检测结果，包含每笔退款的四个日期环节 |
| output/非税核查结果.xlsx | 非税收入核查结果（仅异常记录+汇总行） |
| output/清算核查结果.xlsx | 清算核查可疑记录 |
| output/事实认定书_XXXX.docx | 生成的执法文书 |
| 零余额账户延迟清算积数.xlsx | 延迟清算积数计算结果 |

---

## 配置说明

### 命令行参数（main.py）

| 参数 | 默认值 | 说明 |
|---|---|---|
| `--zero-balance` | 零余额账户.xlsx | 零余额账户文件路径 |
| `--qing-suan` | 集中支付零余额清算待转.xlsx | 垫款户文件路径 |
| `--advance-name` | 集中支付零余额清算待转 | 垫款户名称（对方户名） |
| `--output-dir` | output | 输出目录 |
| `--skip-report` | False | 跳过文书生成 |
| `--report-mode` | template | template 或 llm |
| `--template-path` | -- | 自定义模板路径 |
| `--non-tax` | False | 启用非税核查 |
| `--non-tax-days` | 2 | 非税核查阈值天数 |
| `--settlement-check` | -- | 启用清算核查（指定流水文件） |
| `--settlement-days` | 2 | 清算核查阈值天数 |
| `--workers` / `-w` | 1 | 并行进程数 |
| `--from-results` | -- | 从已有结果文件生成文书（跳过检测） |

### LLM 配置（GUI/代码内）

| 参数 | 默认值 | 说明 |
|---|---|---|
| API Key | DEEPSEEK_API_KEY 环境变量 | DeepSeek API 密钥（留空用占位符） |
| API Base URL | https://api.deepseek.com | API 端点地址 |
| Model | deepseek-chat | 模型名称 |
| Temperature | 0.3 | 生成温度 |
| Max Tokens | 2048 | 最大输出长度 |

---

## 打包部署

使用 PyInstaller 打包为单文件 exe：

**64位：**
```bash
# build_win7.bat
pyinstaller --onefile --windowed --name 执法检测系统_Win_x64 --add-data "事实认定书模板.docx;." --icon=my.ico --hidden-import openpyxl --hidden-import docx --hidden-import pandas main.py
```

**32位（Win7兼容）：**
```bash
# build_win7_32.bat
pyinstaller --onefile --windowed --name 执法检测系统_Win32 --add-data "事实认定书模板.docx;." --icon=my.ico --hidden-import openpyxl --hidden-import docx --hidden-import pandas gui.py
```

**注意：**
- 打包时需包含 `事实认定书模板.docx` 作为资源文件
- `--add-data` 后的分隔符 Windows 使用 `;`，Linux/Mac 使用 `:`
- 打包后的 exe 位于 `dist/` 目录
- `.exe` 运行时通过 `sys.executable` 路径查找资源文件，通过 `BASE_DIR` 变量定位模板

---

## 版本历史

| 日期 | 变更 |
|---|---|
| 2026-07-16 | 新增流水预处理 + 延迟清算检测功能 |
| 2026-07-XX | v2 非税核查算法：FIFO累计匹配 + 余额归零检测 |
| 2026-07-XX | 清算核查功能（仅输出可疑记录） |
| 2026-07-XX | 预索引优化：O(N×P) → O(N log N + P log N) |
| 2026-07-XX | CSV 多编码自动检测 + 列名归一化 |
